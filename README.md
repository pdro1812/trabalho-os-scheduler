### Passos para execução:

**1. Inicie a aplicação:**
Abra o seu terminal na pasta raiz do projeto (onde está o arquivo `docker-compose.yml`) e execute o comando abaixo:

```bash
cd trabalho-os-scheduler
docker-compose up --build
